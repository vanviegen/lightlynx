{
  "name": "lightlynx",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "vite build src/ --outDir ../public --emptyOutDir --assetsDir .",
    "build:debug": "vite build src/ --outDir ../public --emptyOutDir --assetsDir . --mode development --sourcemap --minify false",
    "watch": "npm run build -- --watch",
    "dev": "vite src/ --host",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "npm run build && dotenv -- bash -c 'rclone sync public/ :sftp,host=$DEPLOY_HOST,user=$DEPLOY_USER,pass=$DEPLOY_PASS_OBSCURED,key_use_agent=false:/ --progress --inplace --sftp-disable-hashcheck'"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aberdeen": "^1.3.2",
    "ws": "^8.19.0"
  },
  "devDependencies": {
    "@parcel/packager-raw-url": "^2.13.3",
    "@parcel/transformer-webmanifest": "^2.13.3",
    "dotenv-cli": "^11.0.0",
    "vite": "^6.0.7"
  }
}
